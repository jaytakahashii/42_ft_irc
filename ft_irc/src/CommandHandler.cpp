#include "CommandHandler.hpp"

#include <iostream>

void CommandHandler::handleCommand(Server& server, Client& client,
                                   const std::string& line) {
  // 今後ここにコマンド処理を追加（NICK, JOIN, PRIVMSG など）
}
